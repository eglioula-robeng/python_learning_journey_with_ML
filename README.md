# python_learning_journey_with_ML
Learning Python and getting ready in topics like ML, RL, ROS2. etc. 

# scikit-learn (my own notes)

So first of all we need to install all our packages. Four scikit-learn we need: scikit-learn, numpy (for working with arrays) , pandas (for working with dataframes), matplotlib (for visualisation)
```python
pip3 install scikit-learn numpy pandas matplotlib 
```
